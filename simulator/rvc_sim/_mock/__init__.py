"""Mock RVCController implementation matching the assumed Phase 3 API.

This entire package is removable once the real pybind11 binding is in place;
``rvc_sim.rvc`` will then resolve to the real ``rvc`` module instead.
"""
